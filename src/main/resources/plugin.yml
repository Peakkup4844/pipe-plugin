name: PipePlugin
version: '1.0.1'
main: com.peakkup.pipeplugin.PipePlugin
api-version: '1.20'
folia-supported: true
# Not a dependency — only used so storage plugins are enabled before us and can be
# detected (see skip-plugin-managed-chests). The plugin loads fine without any of them.
softdepend: [WildChests]
author: peakkup
description: Create-style item pipe system (sticky piston input -> glass tube -> piston output).
