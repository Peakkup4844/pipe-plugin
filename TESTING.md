# PipePlugin — In-game Test Checklist

รายการนี้เหลือ **เฉพาะสิ่งที่ต้องทดสอบบนเซิร์ฟเวอร์จริงเท่านั้น** (มี inventory / redstone / piston /
item frame / หลาย platform จริง) — ตรรกะล้วน ๆ ถูกครอบด้วย unit test แล้ว รันด้วย `.\gradlew.bat test`

> **✅ Unit test ครอบคลุมแล้ว (ไม่ต้องเทสมือ):**
> - **Routing** — ใกล้ก่อนไกล, ซ้ายก่อนขวา, หลาย output ต่อกระจกก้อนเดียว (`PipeRouterTest`)
> - **Filter gate** — frame บน input กันการดูด, บนกระจกกั้นแล้ว reroute ไปกิ่งอื่น, บน output ปฏิเสธของไม่ตรง, union หลาย frame, ไม่มี frame = ผ่าน (`PipeRouterTest`, `FrameIndexTest`)
> - **Glass detection** — GLASS/TINTED + stained ครบ 16 สี = ท่อ, pane/ของอื่น = ไม่ใช่ (`NetworkDiscoveryTest`)
> - **Config** — default, clamp ค่าติดลบ, `skip-plugin-managed-chests` (`PipeConfigTest`)
> - **`allowed-containers` ทุกรูปแบบ** — list, `all` (ทุกตัวพิมพ์), **ค่าเดี่ยว `CHEST`**, หลายตัวคั่นจุลภาค,
>   ชื่อผิด/ไม่ใช่บล็อกถูกข้ามพร้อม warning, list ที่ผิดหมดต้อง **ปิดทุกกล่อง ไม่ใช่เปิดทุกกล่อง** (`PipeConfigTest`)
> - **ข้อความ lang.yml ตกไปใช้ของที่ฝังใน jar** — เซิร์ฟที่มี lang.yml เก่าอยู่แล้วต้องยังได้ข้อความของ key
>   ที่เพิ่งเพิ่ม ไม่ใช่พ่นชื่อ key ดิบ ๆ + key ทุกตัวที่โค้ดเรียกต้องมีอยู่จริงในไฟล์ (`PipeLangTest`)
> - **Match mode** — TYPE เทียบชนิด, SIMILAR เทียบ NBT (`MatchModeTest`)
> - **Cache invalidation** — invalidate ทุกบล็อกของท่อ, **บล็อกที่ใช้ร่วมหลายท่อ invalidate ครบทุกเส้น**, re-put ไม่ทิ้ง index ค้าง (`NetworkRegistryTest`)
> - **ดูด/คืนของระดับช่อง** — คืนเข้าช่องเดิม, ช่องถูกยึด, กล่องหด, conservation 10 pulse ผังไม่เพี้ยน (`InventoryOpsTest`)
> - **ด่านกันของหาย/ของเพิ่มที่ต้นทาง** — `countMatching` + จำลอง inventory ที่ "โกหก" **สองแบบ**
>   (เขียนแล้วไม่มีผล / หักของจริงแต่วาดสต๊าคกลับ) ที่อ่านแล้วเหมือนกันเป๊ะ (`InventoryOpsTest`)
> - **ตาข่ายกันกล่องปลั๊กอิน (ชั้นที่ 2)** — คลาส inventory ของเซิร์ฟเวอร์ผ่าน / ของปลั๊กอินถูกปฏิเสธ (`ExternalStorageGuardTest`)
> - **บัญชีดำระหว่างรัน (ชั้นที่ 3)** — จับได้แล้วปิดกล่องนั้นถาวร, เตือนครั้งเดียว, ไม่ลามไปกล่องข้าง ๆ/คนละโลก (`ContainerAccessTest`)
> - **นับของจาก "เจ้าของกล่อง" ไม่ใช่สต๊าคที่วาดโชว์** — จำลอง storage unit ที่โชว์ 64 ค้างไว้ ต้องยังวัดได้ว่าหายไป 32,
>   จำนวนเกิน `int` ก็ยังวัดผลต่างได้เป๊ะ, กล่องธรรมดาต้องตกกลับไปนับ inventory ตามเดิม (`ContainerAccessTest`)
> - **ENDER_CHEST ถูกปฏิเสธถาวร** แม้ระบุใน `allowed-containers` (`PipeConfigTest`)
> - **เทียบโทโพโลยี + รับช่วงล็อก** — output ใหม่ = โทโพโลยีใหม่, รอบเก่ายังวิ่ง → รอบใหม่ถูกกัน (`PipeNetworkTest`)
> - **ลำดับทิศ** (`DirectionsTest`)

**✅ ไม่เหลือข้อที่ต้องเทสมือแล้ว** — ข้อสุดท้าย (**0r** กล่องของปลั๊กอิน storage บนเซิร์ฟจริง)
เจ้าของเซิร์ฟยืนยันแล้วว่าปกติ โดยรันบนเซิร์ฟที่มี **WildChests ลงอยู่จริง** (console ขึ้น `Linked to WildChests.`)
ที่เหลือรันอัตโนมัติหมด — §-1 รอบ hardening, §9 platform 4 ตัว, 4f/4g (`allowed-containers` จาก 0u),
6d, และ 0e/6b (ดูคำอธิบายในข้อนั้น)

> **สิ่งเดียวที่ยังไม่มีใครยืนยัน** ไม่ใช่ข้อเทส แต่คือ *storage unit ของ WildChests เวอร์ชันนี้อ่านจำนวนจริงได้ไหม* —
> jar ที่เจ้าของเซิร์ฟรันตอนยืนยันเป็นตัวก่อนแก้ log จึงแยกไม่ออกระหว่าง "อ่านได้" กับ "อ่านไม่ได้แล้วข้ามไป"
> (ทั้งสองทางของไม่หายไม่เพิ่ม ต่างกันแค่ท่อใช้ storage unit ได้หรือไม่)
> เอา jar ใหม่ลงแล้วดูบรรทัด `Linked to WildChests` อีกครั้ง: ยังเป็น `INFO` = อ่านได้ครบ, กลายเป็น `WARN` = เวอร์ชันไม่ตรง

---

## §-1. รอบ hardening (lite + ปิดช่องโหว่) — รันบนเซิร์ฟจริงครบแล้ว

ผลรัน: **Spigot 1.20.1 / Paper 1.20.1 / Paper 26.2 → อย่างละ 35 ข้อ ผ่านหมด**, **Folia 26.1.2 → 5 ข้อ ผ่านหมด**,
unit test **117 ข้อ**, console ไม่มี exception/warning ของปลั๊กอินเลยทั้ง 4 เซิร์ฟ

- [x] **H1 — `filter-match: TYPE` เคยแปลงของได้** ท่อดูดของเป็น "จำนวน + proto ตัวเดียว" แล้วสร้างสต๊าคใหม่
  จาก proto นั้น พอ TYPE นับ "ลูกศรอาบยาคนละแบบ" เป็นชนิดเดียวกัน ของทั้งกล่องจะออกมาเป็นแบบของตัวแรก
  → ผู้เล่นวางของแพงไว้ช่องแรกแล้วแปลงของถูกเป็นของแพงได้
  **แก้:** การจับคู่ภายในเทียบ `isSimilar` เป๊ะเสมอ โหมด filter เหลือใช้แค่กับ item frame
  → ล็อกด้วย `InventoryOpsTest` 2 ข้อใหม่
- [x] **H2 — shulker ซ้อน shulker** `Inventory#addItem` ไม่เช็คกฎ `canPlaceItem` ของ vanilla เลย ท่อจึงทำสิ่งที่
  hopper ในเกมทำไม่ได้ (ใช้ทำ NBT ซ้อนกันจนบวมเพื่อถ่วงเซิร์ฟ/ไคลเอนต์)
  **แก้ + กันท่อตันด้วย:** คัดชนิดตั้งแต่ฝั่งต้นทางจาก Material ของกล่องปลายทาง ไม่ใช่ปฏิเสธตอนยัด
  (ไม่งั้น shulker ใบเดียวในกล่องต้นทางจะล็อกท่อไว้ตลอด)
  > รันจริง: ปลายทาง shulker + ต้นทางมี shulker 5 ใบ + ดิน 320 → ยิง 5 pulse → **ดินไป 160 ตามปกติ**,
  > shulker ค้างที่ต้นทางครบ 5, ไม่มีของตกพื้น; และ shulker ลง chest ธรรมดายังได้ปกติ (3/3)
- [x] **H3 — ท่อลอดเข้าเขตคนอื่นได้** เดิมไม่ยิง event ใด ๆ → WorldGuard/GriefPrevention/Towny/LWC/CoreProtect
  มองไม่เห็นเลย ใครก็เอา sticky piston ไปจ่อกล่องในเขตคนอื่นแล้วดูดของออกมาได้
  **แก้:** ยิง `InventoryMoveItemEvent` ทุกครั้งที่ยัดของ (`call-inventory-move-event`, default true)
  > รันจริงด้วยปลั๊กอินตัวแทน: เห็น `WATCH-MOVE 0,100,0 -> 6,100,0 DIRT x32` ครบทุก pulse
  > และเมื่อสั่ง cancel → **ของไม่ขยับเลย (dest=0) ต้นทางครบ 320 ไม่มีของตกพื้น** ทั้ง 3 platform
- [x] **H4 — 1 pulse เคยกิน ~5 tick** `runAtLocation` บน Spigot/Paper คือ `runTask` = เลื่อนไป tick หน้าเสมอ
  แม้อยู่บนเธรดหลักอยู่แล้ว → ท่อตอบช้าและเปิดช่องให้ hopper/ผู้เล่นแทรกกลางรอบ
  **แก้:** `RegionExecutor` รันทันทีเมื่อ region เป็นของเธรดปัจจุบัน → ทั้งรอบจบใน tick เดียว (atomic)
- [x] **H5 — งานซ้ำจาก redstone** dust หนึ่งเส้นยิง event เป็นชุด แต่ทุกก้อนถามคำถามเดียวกัน
  **แก้:** ยุบเหลืองานเดียวต่อ piston ต่อรอบ + รวม 2 map เป็น map เดียว + เช็คกระจกก่อน `getState()`
  ใน PistonGuard + ออกก่อนถ้ายังไม่มีท่อไหนถูก cache + เลิกล็อก `synchronized` ตอนหา WildChests
- [x] **H6 — Folia เทสได้จริงแล้ว** เดิมบันทึกไว้ว่าเทสไม่ได้เพราะ Folia ไม่มี `/data` และ `/item`
  **วิธีใหม่:** ให้ปลั๊กอินตัวแทน log ยอดรวมของทั้งสองกล่องตอนอีเวนต์ยิง แล้วอ่านกลับจาก console
  > รันจริง: 5 pulse = 5 รอบ, ทุกรอบ `source + destination + ของในมือ = 320` เป๊ะ
  > (288/0, 256/32, 224/64, 192/96, 160/128 — ครั้งละ 32) ไม่มี threading violation ไม่มี exception

---

## §0. ⚡ ยืนยัน fix รอบล่าสุดก่อน

### 0.1 guard เชิงโครงสร้าง (piston ไม่ดันของ) — ✅ ผ่านแล้วรอบก่อน
- [/] **0a** วางกระจกต่อท่อหลัก → piston ไม่ยืด โครงไม่พัง
- [/] **0b** ทุบกระจก/วางบล็อกติดท่อ → piston ไม่ยืด กล่องไม่แตก
- [/] **0c** จ่ายไฟค้างแล้ววาง/ทุบบล็อกรอบ ๆ → piston ไม่ยืด

### 0.2 🆕 ผังกล่องต้นทางต้องไม่เพี้ยน (แก้: คืนของเข้าช่องเดิม)

อาการเดิม: ปลายทางเต็มแล้วยังจ่ายไฟ → ของในกล่องต้นทางค่อย ๆ ถูกยุบมารวมช่องต้น ๆ เอง

- [/] **0d** ต้นทางวางของ **เว้นช่อง** (เช่นช่อง 5 กับช่อง 20) → ทำให้ **ปลายทางเต็มสนิท** → จ่ายไฟค้าง/กดรัว ~20 รอบ → **ของต้องอยู่ช่องเดิมเป๊ะ จำนวนเท่าเดิม ไม่ขยับไปช่อง 1**
- [~] **0e** ระหว่างปลายทางเต็ม ลองเอาของอื่นไปวางทับช่องเดิมกลางคัน → ของที่คืนไม่ได้ต้องไปช่องว่างอื่น (ไม่ทับของคนอื่น ไม่หาย)
  🔧 **ไม่ต้องเทสแล้ว — ทำให้เกิดไม่ได้บน Bukkit/Paper** ลองบังคับด้วย hopper ที่จ่ายของเข้ากล่องต้นทาง
  ระหว่างท่อกำลังทำงาน: hopper ส่งของได้ปกติถ้ามีช่องว่างค้างไว้ (8 ชิ้นใน 3 วิ) แต่ยิง **120 pulse
  แล้วแทรกไม่ได้เลยสักครั้ง** เพราะทั้ง 3 เฟส (ดูด→ใส่→คืน) จบใน **tick เดียวกัน** ไม่มีช่องให้ใครแทรก
  → ครอบด้วย unit test แทน (`InventoryOpsTest`: ช่องถูกยึด → ไปช่องอื่น, กล่องหด → drop ไม่หาย)
  และรันจริงยืนยันแล้วว่า **ไม่มีของถูกทับ/หายเลย** ทั้งบน Spigot 1.20.1 และ Paper 26.2

### 0.3 🆕 เห็น output ใหม่ทันที (แก้: ค้นท่อใหม่ทุก pulse)

อาการเดิม: ต่อท่อไป output ใหม่แล้วมันไม่เห็น ต้องไปทุบบล็อกอื่นก่อนถึงเจอ

- [/] **0f** **ลำดับที่เคยพัง:** วาง output piston ติดกระจกก่อน (ยังไม่มีกล่อง) → กด pulse 1 ที → แล้วค่อยวางกล่องปลายทางหน้า piston → **pulse ถัดไปต้องส่งของเข้ากล่องใหม่ทันที** (ไม่ต้องไปทุบอะไรกระตุ้น)
- [/] **0g** ต่อกระจกยาวออกไปหา output ใหม่ทีละก้อน → pulse ถัดไปหลังต่อครบต้องเจอเลย
- [/] **0h** ย้ายกล่องปลายทาง (ทุบแล้ววางที่ใหม่หน้า piston เดิม) → pulse ถัดไปใช้กล่องใหม่ถูกต้อง
- [/] **0i** ⭐ **`/fill` ลบกระจกกลางท่อ** (ไม่ยิง event) → **pulse ถัดไปต้องหยุดทำงานทันที** (เคสที่เคยไม่ผ่าน §7e)
- [/] **0j** `/fill` ลบแล้ววางกลับ → กลับมาทำงานเองโดยไม่ต้องทุบอะไร

### 0.4 🔧 storage ของปลั๊กอินอื่น (WildChests) — **hook เคยพัง แก้แล้ว ต้องเทสใหม่ทั้งหมด**

> **รอบก่อนไม่ผ่าน:** console ขึ้น `its API could not be read` = กล่อง WildChests **ไม่ถูกป้องกันเลย**
> สาเหตุ: โค้ดเรียก `WildChestsAPI.getChestsManager()` ซึ่งไม่มีอยู่จริง (ตรวจจาก jar 2026.2 ที่ลงไว้แล้ว —
> ของจริงคือ static `getChest` / `getLinkedChest` / `getStorageChest`) ตอนนี้แก้ตรงกับ jar นั้นแล้ว
> และเพิ่มการป้องกันอีก 2 ชั้นที่ไม่พึ่ง API ของเขา

**ทำไมต้องกัน:** ของจริงของ WildChests อยู่ในโครงสร้างของปลั๊กอินเอง ไม่ใช่ใน inventory ของบล็อก
→ ท่อเขียนลงไป = ของหายตอนทุบกล่อง / ท่อดูด "ไอเทมโชว์" ของ storage unit ออกมา = **dupe ไม่จำกัด**

- [/] **0k** ⚠️ **ข้อความเปลี่ยนแล้ว** เปิดเซิร์ฟที่มี WildChests → console ต้องขึ้น **`Linked to WildChests.`**
  (ถ้ายังขึ้น `could not be read` แปลว่ายังไม่ตรง — ส่งบรรทัด log มาให้ดู) — เทสรวมกับ **0y**
- [/] **0l** ใช้ WildChest เป็น **ปลายทาง** → ท่อต้องไม่ส่งของเข้าเลย (ของค้างต้นทาง ครบจำนวน ไม่หาย)
- [/] **0m** ใช้ WildChest เป็น **ต้นทาง** → ท่อไม่ดูดของออก
- [/] **0n** chest **ธรรมดา** ในเซิร์ฟเดียวกัน → ยังทำงานปกติทุกอย่าง (guard ต้องไม่เหวี่ยงแห — ข้อนี้สำคัญพอ ๆ กับ 0l/0m)
- [/] **0o** ✅ **ผ่านแล้วผ่าน 0s/0y/0z** (ข้อนี้ไม่มีวิธีเทสของตัวเอง — มันคือ "ความคาดหวังที่เปลี่ยนไป")
  **storage unit เป็นต้นทาง → ตอนนี้ท่อ*ต้อง*ทำงานได้**
  (เดิมข้อนี้คาดหวังว่าท่อจะไม่แตะ) เหตุผลอยู่ใน §0.5 — เราถามจำนวนจริงจาก WildChests ได้แล้ว
  จึงตรวจการอนุรักษ์ของได้เป๊ะ ปลอดภัยเท่ากล่องธรรมดา วิธีเทสอยู่ที่ **0s / 0y / 0z**
- [/] **0p** 🆕 **linked chest** (กล่องที่ผูกของร่วมกันหลายใบ) เป็นต้นทาง/ปลายทาง → ท่อไม่แตะ
- [/] **0q** 🆕 ตั้ง `skip-plugin-managed-chests: false` → reload → ท่อ**ยอม**ใช้กล่องพวกนั้น
  (ยืนยันว่าสวิตช์ปิดได้จริง — **อย่าใช้ค่านี้บนเซิร์ฟจริง** ทดสอบแล้วเปลี่ยนกลับเป็น `true`)
  🔧 **หมายเหตุ:** สวิตช์นี้ปิดได้แค่ชั้น 1 กับ 2 เท่านั้น **ชั้นที่ 3 (นับของจริง) ปิดไม่ได้** (ตั้งใจ:
  มันคือกฎกันของหาย ไม่ใช่ตัวเลือก) และ **storage unit ใช้งานได้ทั้งค่า `true` และ `false`**
  — ตั้งกลับเป็น `true` ได้เลย ไม่ต้องปิดสวิตช์เพื่อให้ storage unit ทำงานอีกต่อไป
- [x] **0r** ✅ ปลั๊กอิน storage **ตัวอื่น** ที่มีในเซิร์ฟ (sell chest / backpack / กล่องพิเศษอื่น ๆ) →
  **เจ้าของเซิร์ฟรันจริงแล้ว ปกติทั้งหมด ไม่มีของหาย/เพิ่ม และไม่มีคำเตือนของชั้นที่ 3 ใน console**
  (ถ้าวันหลังขึ้น `Skipping a container backed by a plugin-owned inventory (<ชื่อคลาส>)` = ตาข่ายชั้น 2 ทำงาน
  ส่งชื่อคลาสมาได้ จะเพิ่มการตรวจแบบเจาะจงให้)

### 0.5 🔴 **storage unit ใช้งานได้แล้ว (แก้รอบสอง — ต้องยืนยัน)**

> **อาการที่คุณเจอ (สองรอบติด):**
> 1. รอบแรก — ท่อดูดของออกจาก storage unit จนหมด แต่ปลายทางได้แค่ 64 ชิ้น (**ของถูกทำลาย 32 ชิ้นทุก pulse**)
> 2. รอบสอง — กดแล้วไม่มีอะไรเกิดขึ้นเลย ไม่มี particle + log `did not lose the items taken from it
>    (took 32, it lost 0)` ที่ `448,70,-178` (**ท่อขึ้นบัญชีดำกล่องที่จริง ๆ ทำงานถูกต้อง**)
>
> **สาเหตุที่แท้จริง (แกะ bytecode ของ `WildChests-2026.2.jar` แล้ว ไม่ใช่การเดา):**
> storage unit เก็บจำนวนจริงเป็นตัวเลขก้อนเดียว (`amount`) แล้ววาด "สต๊าคโชว์" `min(amount, 64)`
> ลงช่องเดิมเสมอ พอท่อเขียนสต๊าค 32 ทับสต๊าค 64 มันจะ **หักของจริง 32 แล้ววาด 64 กลับมาทันที**
> → เรานับ inventory ซ้ำได้ 64 เท่าเดิม เลยอ่านว่า "lost 0" ทั้งที่ของหายไปจริง 32
>
> - รอบแรกเราสรุปว่า "ของยังอยู่" แล้ว**ทิ้ง**ของในมือ → ของหายจริงทุก pulse
> - รอบสองเราเลิกทิ้ง (ยัดกลับหมด) แต่ยัง**อ่านผิดอยู่** เลยขึ้นบัญชีดำ → ท่อตายสนิท
>
> **แก้รอบนี้ (แก้ที่ต้นเหตุ):** เลิกนับจาก inventory สำหรับกล่องพวกนี้ แล้ว **ถาม WildChests
> เอาจำนวนจริงมาเลย** (`StorageChest#getAmount()`) → ตรวจการอนุรักษ์ของได้เป๊ะทั้งขาเข้า/ขาออก
> → **storage unit ใช้งานกับท่อได้ตามปกติแล้ว** ทั้งเป็นต้นทางและปลายทาง และใช้ได้แม้ตั้ง
> `skip-plugin-managed-chests: true` (ค่าปลอดภัย) — ไม่ต้องปิดสวิตช์อีกต่อไป

**ก่อนเทส:** ต้อง **รีสตาร์ตเซิร์ฟ** (บัญชีดำเดิมอยู่ในหน่วยความจำ) และอัปเดต jar ใหม่

- [/] **0s** ⭐⭐ **ทำซ้ำเคสเดิมที่ `448,70,-178`** (storage unit เป็นต้นทาง):
  จดจำนวนในกล่อง → กด pulse 5 ครั้ง → **ต้นทางต้องลดลง 32×5 = 160 พอดี และปลายทางต้องได้เพิ่ม 160 พอดี**
  (ก่อนหน้านี้ปลายทางค้างที่ 64 ตลอด) — ตัวเลขต้องตรงเป๊ะทั้งสองฝั่ง
- [/] **0t** ⭐ ระหว่าง 0s console ต้อง **ไม่มี** `did not lose the items taken from it` เลยแม้แต่บรรทัดเดียว
  ถ้ายังขึ้น → **ก๊อปทั้งบรรทัดมา** พร้อมบอกว่ากล่องนั้นเป็น storage unit ของ WildChests หรือปลั๊กอินอื่น
- [/] **0y** ⭐ ตอนเปิดเซิร์ฟ console ต้องขึ้น `Linked to WildChests. Its storage units can be read directly...`
  ถ้าขึ้น `could not be read` แทน = hook พัง แล้ว storage unit จะไม่ทำงาน (ส่งบรรทัดมาให้ดู)
- [/] **0z** ⭐ **storage unit เป็นปลายทาง** → กด pulse หลายครั้ง → ตัวเลขในกล่องต้องเพิ่มขึ้นเท่าที่ส่งไปเป๊ะ
  และต้องไม่มี log `accepted N item(s) but only gained M`
- [/] **0z2** ⭐ **ปลายทางเป็น storage unit ที่เก็บของ*คนละชนิด*** (เช่น unit เก็บหิน แต่ต้นทางมีดิน) →
  ท่อต้อง **ข้ามไปเฉย ๆ** ไม่ส่งเข้า: ของต้องคืนต้นทางครบ, **ห้ามมีของกองบนพื้น**, ห้ามมี log เตือน
  และ unit นั้นต้องยัง**ใช้งานได้ต่อ** (ลองส่งของชนิดที่ถูกไปดู ต้องเข้าปกติ)
- [/] **0s2** 🔒 ตั้ง `skip-plugin-managed-chests: **true**` (ค่าปลอดภัย) → รีสตาร์ต → ทำ 0s ซ้ำ
  **ต้องได้ผลเหมือนกันทุกอย่าง** (นี่คือประเด็นหลักของการแก้รอบนี้ — ไม่ต้องปิดสวิตช์เพื่อใช้ storage unit)
- [/] **0u** ยืนยันว่า**บัญชีดำยังทำงาน**อยู่: ถ้าเจอกล่องที่ log เตือนขึ้นจริง ท่อเส้นนั้นต้องหยุดถาวร
  จนกว่าจะรีสตาร์ต — **นี่คืออาการที่ถูกต้อง ไม่ใช่บั๊ก** (แต่กับ storage unit ต้องไม่เกิดขึ้นแล้ว)
  📌 **หมายเหตุที่คุณฝากไว้ตรงนี้ ("ใส่ allow แค่ chest แต่ black_shulker ยังทำงาน") = บั๊กจริง แก้แล้ว**
  ทำซ้ำได้บนเซิร์ฟจริงทั้ง 2 สาเหตุ ดูรายละเอียดและวิธีเทสที่ **§4 ข้อ 4f/4g**
- [/] **0w** กล่องปกติทุกชนิด (chest/barrel/shulker/hopper) → ต้อง**ไม่**มี log เตือนทั้งสองแบบโผล่มาเลย
  (ถ้าโผล่กับกล่องธรรมดา = เตือนผิด ให้แจ้งทันที เพราะจะทำให้ท่อดี ๆ ตายฟรี)
- [/] **0x** **ยืนยันของไม่หายรวม:** นับ (ต้นทาง + ปลายทาง + ของที่ drop พื้น) ก่อน/หลัง 0s
  ผลรวมต้องเท่ากันเป๊ะ — **ห้ามน้อยลงและห้ามมากขึ้น**
- [/] **0v** WildChests **chest ปกติ / linked chest** → ต้องยัง **ไม่ถูกแตะ** เหมือนเดิม (0l/0m/0p)
  ข้อยกเว้นที่เพิ่มมามีแค่ storage unit เท่านั้น

## §1. Smoke (ไม่ผ่าน = หยุด)

- [/] **1a** โยน jar ลง Paper ล่าสุด → start: console `Enabled on platform: Bukkit/Paper`, ไม่มี error/stacktrace
- [/] **1b** `plugins/PipePlugin/` มี `config.yml` + `lang.yml` ครบ
- [/] **1c** chest(มีของ) ← sticky piston / กระจกสีเดียว / piston → chest ปลายทาง, กด button → ของย้าย 1 ชนิด สูงสุด 32
- [/] **1d** กด button ซ้ำ ๆ → ย้ายทีละรอบ ไม่ค้าง ไม่ซ้อน
- [/] **1e** ดู piston ขณะทำงาน → **ไม่ยืดออกมา** (เป็นแค่ marker)

## §2. Topology discovery (ต้องใช้บล็อกจริง — `discover()` ไม่ได้ unit test)

- [/] **2a** กระจกคนละสีวางติดกัน → **ไม่**เชื่อมเป็นท่อเดียว (แต่ละสี = คนละเครือข่าย)
- [/] **2b** `GLASS` ใส + `TINTED_GLASS` → ใช้เป็นท่อได้ (แต่ละอันเป็นเครือข่ายของตัวเอง)
- [/] **2c** กระจก **pane** (แผ่นบาง) → ใช้เป็นท่อไม่ได้
- [/] **2d** sticky piston ติดกระจก **2 สี** พร้อมกัน → reject (ท่อไม่ทำงาน)
- [/] **2e** ท่อยาวเกิน `max-pipe-length` (ตั้ง 8 แล้วต่อ 12 บล็อก) → ไม่ทำงาน
- [/] **2f** เว้นช่องว่าง 1 บล็อกกลางท่อ → ไม่ทำงาน
- [/] **2g** double chest ต้นทาง/ปลายทาง → ใช้ inventory รวมถูกต้อง

## §3. Redstone trigger + rate limit (ต้องใช้วงจรจริง)

- [/] **3a** lever เปิด→ปิด→เปิด → นับเฉพาะขาขึ้น (2 รอบ)
- [/] **3b** button = 1 รอบ/กด
- [/] **3c** observer / repeater / comparator จ่ายไฟ → ทำงานได้
- [/] **3d** redstone clock เร็ว (default `min-pulse-interval-ticks: 2`) → ถูกจำกัด ~10 รอบ/วิ ไม่รัวเกิน
- [/] **3e** `min-pulse-interval-ticks: 20` → สูงสุด 1 รอบ/วิ
- [/] **3f** `min-pulse-interval-ticks: 0` → ยิงทุก pulse ไม่จำกัด
- [/] **3g** sticky piston ธรรมดา (ประตู/กลไกอื่นที่ไม่มีกระจกท่อ) → plugin ไม่แตะ ไม่แลค

## §4. Container จริง (end-to-end)

- [/] **4a** barrel / shulker box / dropper / dispenser เป็นต้นทาง/ปลายทาง → ใช้ได้ทั้งหมด
- [/] **4b** `allowed-containers: [CHEST, BARREL]` → hopper ใช้เป็นต้นทาง/ปลายทาง**ไม่ได้**
- [/] **4c** **hopper** เป็นต้นทาง/ปลายทาง → ย้ายของได้ (⚠️ hopper ขยับของเองทุก ~8 tick แบบ vanilla — ปกติ ไม่ใช่บั๊ก plugin; ดูข้อ 6d เพื่อยืนยันว่าไม่ dupe)
- [ ] **4d** กล่องจากปลั๊กอิน storage อื่น ๆ → ย้ายไปเทสรวมที่ **0r** แล้ว
- [x] **4e** 🆕 **ender chest** เป็นต้นทาง/ปลายทาง (ลองใส่ `ENDER_CHEST` ใน `allowed-containers` ด้วย) →
  ท่อต้อง**ไม่ทำงานกับมันเด็ดขาด** (inventory ของมันเป็นของผู้เล่นที่เปิด ไม่ใช่ของบล็อก = ทั้ง dupe และขโมยของคนอื่น)
  ✅ รันอัตโนมัติครบทั้ง 4 platform แล้วใน §9 (ต้นทางไม่ขยับเลย ไม่มีของตกพื้น)

### 🔴 4f/4g — บั๊ก `allowed-containers` ที่คุณเจอ (แก้แล้ว ต้องยืนยัน)

> **อาการ:** ตั้ง allow แค่ CHEST แต่ `black_shulker_box` ยังใช้ได้ **โดยไม่มี warning อะไรเลย**
>
> ทำซ้ำบนเซิร์ฟจริงแล้ว เจอ **2 สาเหตุ** ที่ทำให้ค่าที่ตั้งถูกทิ้งเงียบ ๆ:
> 1. **เขียน `allowed-containers:` ซ้ำสอง key** — ในไฟล์เดิมมีตัวอย่างที่ comment ไว้ว่า
>    `# allowed-containers:` / `#   - CHEST` อยู่**เหนือ**บรรทัดจริง `allowed-containers: all`
>    พอเอา `#` ออกก็ได้ key ซ้ำ แล้ว **YAML เก็บแค่ตัวท้ายสุด** → กลายเป็น `all` เหมือนเดิม
>    ปลั๊กอินมองไม่เห็นความซ้ำเลย เพราะ YAML ตัดทิ้งไปก่อนถึงมือเรา
> 2. **เขียนค่าเดี่ยว `allowed-containers: CHEST`** (ไม่ใช่ list) — เดิมโค้ดตีว่า "ไม่ใช่ list = all"
>
> **แก้แล้ว 3 อย่าง:** (ก) ค่าเดี่ยวใช้ได้จริง เขียนหลายตัวคั่นจุลภาคก็ได้ (ข) ไฟล์ config เขียนตัวอย่าง
> ใหม่ให้ไม่กลายเป็น key ซ้ำ พร้อมเตือนเรื่อง YAML ตรงนั้น (ค) **log ค่าที่ใช้จริงทุกครั้งที่เปิดเซิร์ฟ**
> — อันนี้คือตัวจับได้ทุกกรณี รวมถึง key ซ้ำที่ปลั๊กอินตรวจเองไม่ได้

- [x] **4f** ตั้ง `allowed-containers: CHEST` (บรรทัดเดียว ไม่ใช่ list) → รีสตาร์ต →
  console ต้องขึ้น **`Allowed containers: CHEST`** → chest ใช้ได้, **barrel/shulker ต้องใช้ไม่ได้**
- [x] **4g** ตั้งเป็น list จริง (`- CHEST` / `- BARREL`) → console ขึ้น `Allowed containers: CHEST, BARREL`
  → ทั้งสองใช้ได้ hopper ใช้ไม่ได้; แล้วลองพิมพ์ชื่อผิด (`- CHESTT`) → ต้องมี warning
  `'CHESTT' is not a block material (skipped)` และตัวที่เหลือยังทำงาน
  ⚠️ **ถ้าบรรทัด `Allowed containers:` ไม่ตรงกับที่ตั้งไว้ = ตั้งไม่ติด** ให้ดูว่ามี `allowed-containers:`
  ซ้ำสองที่ในไฟล์หรือเปล่า

> ✅ **รันเองบนเซิร์ฟจริงแล้ว (Paper 1.20.1) — รีสตาร์ตใหม่ทุกแบบ** แต่ละรอบดูทั้งบรรทัดที่ปลั๊กอิน log
> และลองจริงว่ากล่องชนิดไหนขยับของได้บ้าง (ตรวจทั้ง**ต้นทางและปลายทาง**):
>
> | ที่เขียนใน config.yml | บรรทัดที่ log ออกมา | chest | barrel | shulker | hopper |
> |---|---|---|---|---|---|
> | `allowed-containers: CHEST` | `Allowed containers: CHEST` | ✅ | ✕ | ✕ | ✕ |
> | `allowed-containers: CHEST, BARREL` | `Allowed containers: CHEST, BARREL` | ✅ | ✅ | ✕ | ✕ |
> | list `- CHEST` / `- BARREL` | `Allowed containers: CHEST, BARREL` | ✅ | ✅ | ✕ | ✕ |
> | list `- CHESTT` / `- BARREL` (พิมพ์ผิด) | warning `'CHESTT' is not a block material (skipped)` + `Allowed containers: BARREL` | ✕ | ✅ | ✕ | ✕ |
> | key ซ้ำสองอัน (กับดักเดิม) | `Allowed containers: all` | ✅ | ✅ | ✅ | ✅ |
>
> แถวสุดท้ายคือประเด็น: ปลั๊กอินตรวจ key ซ้ำเองไม่ได้ (YAML ตัดทิ้งไปก่อน) **แต่บรรทัด log ฟ้องตรง ๆ**
> ว่ามันใช้ `all` อยู่ — ดูบรรทัดเดียวก็รู้ทันที
> แถว `CHESTT` ที่ chest ขึ้น ✕ ถูกต้องแล้ว: ชื่อพิมพ์ผิดถูกทิ้ง เหลือแต่ BARREL จริง ๆ

## §5. Item frame จริง (การเกาะ/อ่านสด — สแกน entity จริง)

- [x] **5a** frame ใส่ diamond บนกระจกกลางท่อ → มีแต่ diamond ผ่านจุดนั้น (ของอื่นไปต่อไม่ได้)
  ✅ รันอัตโนมัติซ้ำแล้วด้วย (`summon item_frame` ผ่าน RCON) บน Spigot 1.20.1 + Paper 26.2 — ดู 6d
- [/] **5b** frame บน **output piston** → output นั้นรับเฉพาะของที่ตรง
- [/] **5c** frame บน **sticky piston (input)** → ดูดเฉพาะของที่ตรง
- [/] **5d** frame **หันคนละด้าน** ของกระจกก้อนเดียว (บน/ล่าง/4 ข้าง) → ยังคุมกระจกก้อนนั้นถูก (ทดสอบ `getAttachedFace` ทุกทิศ)
- [/] **5e** เอาไอเทมออกจาก frame → รอบถัดไปผ่านหมดทันที (อ่านสดทุก pulse ไม่ต้อง reload)
- [/] **5f** frame ว่าง (ไม่มีไอเทม) บนกระจก → เหมือนไม่มี gate (ผ่านหมด)
- [/] **5g** `filter-match: TYPE` → ดาบ enchant match frame ดาบเปล่า; ตั้ง `SIMILAR` → ไม่ match

## §6. Item safety — นับของจริง (สำคัญสุด: ห้ามหาย/ห้ามซ้ำ)

- [/] **6a** chest ปลายทางเต็ม → ของกลับต้นทางครบ ไม่ drop พื้น (🆕 ต้องกลับ **ช่องเดิม** ด้วย — ดู 0d)
- [~] **6b** ปลายทางเต็ม **+ ต้นทางเต็มพอดี** → ของ drop ที่พื้นเหนือต้นทาง (ไม่หายเข้ากลีบเมฆ)
  🔧 **เกิดไม่ได้บน Bukkit/Paper — และนั่นคือเรื่องดี** ท่อคืนของ**เข้าช่องที่มันดูดออกมาเอง** ช่องนั้นจึง
  ว่างเสมอโดยธรรมชาติ → ต้นทางจะ "เต็มจนคืนไม่ลง" ได้ก็ต่อเมื่อมีคนอื่นมายึดช่องนั้นระหว่างรอบ
  ซึ่งเป็นไปไม่ได้เพราะทั้งรอบจบใน tick เดียว (ทดลองด้วย hopper แล้ว ดู **0e**)
  ทางลง drop จึงเป็น**ตาข่ายกันของหายเผื่อปลั๊กอินอื่นมายุ่ง** ไม่ใช่ทางเดินปกติ — ครอบด้วย
  `InventoryOpsTest` (กล่องหด) และรันจริงยืนยันแล้วว่าของครบ 32/32 ไม่หายแม้แต่ชิ้นเดียว
- [/] **6c** ต้นทางว่าง → ไม่มีอะไรเกิด ไม่มีเอฟเฟกต์ ไม่ error
- [x] **6d** **นับ total ก่อน/หลัง** หลาย ๆ pulse → เท่าเดิมเป๊ะ (เทสทั้งกรณีมี filter กั้นบางชนิด และกรณี hopper ต้นทางที่**ตัด vanilla path ทั้งหมด** เพื่อยืนยันว่า plugin ไม่ dupe)
  ✅ **รันอัตโนมัติแล้วทั้ง Spigot 1.20.1 และ Paper 26.2** (ผ่านทั้งคู่)
  - **มี filter:** frame ใส่ดิน บนกระจกกลางท่อ, ต้นทางมีดิน 320 + หินกรวด 320 → 10 pulse →
    ดินถึงปลายทาง **320 พอดี**, หินกรวดค้างต้นทาง **320 พอดี** (ไม่มีหลุดผ่าน gate เลย),
    รวมก่อน/หลัง **640 = 640**, ไม่มีของตกพื้น
  - **hopper ต้นทาง ตัด vanilla path:** hopper หันลงใส่หิน (ดันไม่ได้) + ไม่มีอะไรอยู่บน (ดูดไม่ได้)
    → ของที่ขยับมีแต่ฝีมือปลั๊กอิน → 10 pulse ย้าย **320 พอดี** รวมก่อน/หลังเท่าเดิม ไม่มีของตกพื้น
- [/] **6e** ของมี NBT (ดาบ enchant / ไอเทมเปลี่ยนชื่อ) → ไม่ถูกรวมกับของธรรมดา (โหมด SIMILAR)
- [/] **6f** ของ stack ไม่ได้ (ดาบ/เกราะ/shulker) → ย้าย **1 ชิ้น/pulse**
- [/] **6g** ต้นทางมี 5 ชนิด → กด 5 pulse ถึงย้ายครบทุกชนิด (1 ชนิด/pulse)
- [/] **6h** output หลายตัว: ตัวใกล้เต็มก่อน แล้วล้นไปตัวถัดไปในรอบเดียว; ทุกตัวเต็ม → ของกลับต้นทาง

## §7. Invalidation ในโลกจริง

- [/] **7a** ทุบกระจกกลางท่อ → pulse ถัดไปหยุดทำงาน (ไม่เดินผ่านท่อที่พัง)
- [/] **7b** ต่อ output piston เพิ่มข้างกระจก → pulse ถัดไปเห็น output ใหม่
- [/] **7c** ทุบ chest ปลายทาง / เปลี่ยนเป็นบล็อกธรรมดา → ไม่ error, ไม่มีของหาย
- [/] **7d** TNT ระเบิดใส่ท่อ → invalidate ถูก ไม่ error
- [~] **7e** ~~ลบกระจกด้วย WorldEdit/`/fill` → ไม่หยุดทำงาน~~ 🔧 **แก้แล้ว** — ตอนนี้ค้นท่อใหม่ทุก pulse ไม่เชื่อ cache → เทสซ้ำที่ **0i / 0j**

## §8. Effects

- [/] **8a** ย้ายของสำเร็จ → มีเสียง + particle ที่**ต้นทางและปลายทาง**
- [/] **8b** ต้นทางว่าง (ไม่มีอะไรย้าย) → **ไม่มี**เอฟเฟกต์ (แยกออกว่าท่อทำงานจริง)
- [/] **8c** `effects: false` → เงียบสนิท ไม่มี particle/เสียง
- [x] **8d** เซิร์ฟเวอร์เวอร์ชันใหม่ → เอฟเฟกต์ไม่ error แม้ชื่อ Particle/Sound เปลี่ยน
  ✅ **รันบน Paper 26.2 จริงแล้ว** (ใหม่กว่า 26.1) การย้ายของยังทำงานครบ ไม่มี exception ใน console
  — `Particle.CRIT` และ `Sound.BLOCK_DISPENSER_DISPENSE` ยังมีอยู่ในเวอร์ชันนั้น ดู §9b

## §9. Platform (jar เดียวต้องครบทุกอัน) — ✅ **รันจริงครบแล้ว ไม่ต้องเทสมือ**

> เซิร์ฟเวอร์จริงทั้ง 4 ตัวถูกโหลดมารันเอง แล้วสั่งงานผ่าน RCON ทั้งหมด (สร้างท่อด้วย `/setblock`,
> จ่ายไฟด้วยการวาง/ลบ redstone block, นับของทีละช่องด้วย `/data get`) — jar เดียวกับที่ส่งให้
>
> | platform | เวอร์ชันที่รัน | Java | ผล |
> |---|---|---|---|
> | **Spigot แท้** (build เองด้วย BuildTools) | 1.20.1 | 21 | **14/14 ผ่าน** |
> | Paper | 1.20.1 | 21 | **14/14 ผ่าน** |
> | Paper | **26.2** (ใหม่กว่าที่ README เคยเขียนไว้) | 25 | **14/14 ผ่าน** |
> | **Folia** | 26.1.2 | 25 | ผ่าน (วิธีวัดต่างออกไป — ดู 9c) |
>
> 14 ข้อที่รันอัตโนมัติต่อ platform: ต้นทางเริ่ม 320 ชิ้น · ปลายทางว่าง · **5 pulse ย้าย 160 พอดี** ·
> ผลรวมก่อน/หลังเท่ากันเป๊ะ · ไม่มีของตกพื้น · shulker เป็นปลายทางได้ · **ender chest ไม่ถูกแตะ** ·
> ปลายทางเต็มแล้วต้นทางไม่ขยับ · ของ stack ไม่ได้ย้าย 1 ชิ้น/pulse · กระจกคนละสีคั่น = ท่อตาย ·
> เปลี่ยนสีกลับแล้วใช้ได้ทันทีโดยไม่ reload · เอฟเฟกต์เปิดอยู่แล้วการย้ายยังทำงาน

- [x] **9a** **Spigot 1.20.1 แท้** (ไม่ใช่ Paper) — build จาก BuildTools แล้วรันจริง: โหลดได้ ไม่มี error
  และผ่านครบ 14/14 ยืนยันว่าไม่มีการเรียก API ที่มีเฉพาะ Paper
- [x] **9b** **Paper 26.2** (เวอร์ชันใหม่สุดตอนนี้ ใหม่กว่า 26.1 ที่เคยระบุไว้): ผ่าน 14/14
  รวม **effects** — `Particle.CRIT` / `Sound.BLOCK_DISPENSER_DISPENSE` ยังมีอยู่ ไม่โยน exception
  (ครอบข้อ **8d** ที่ค้างอยู่ไปด้วย)
- [x] **9c** **Folia 26.1.2**: ขึ้น `Enabled on platform: Folia` ถูกต้อง, ท่อยาว 32 บล็อกพาดข้าม 3 chunk,
  กด 5 pulse → **ต้นทาง 320→160 ปลายทาง 0→160 เป๊ะ**, ไม่มีของตกพื้น, ไม่มี exception/thread error
  และไม่มี warning ของปลั๊กอินเลย
  🔧 Folia ไม่มีคำสั่ง `/data` กับ `/item` และ `/execute if block|data` ก็ throw เมื่อสั่งจาก console
  จึงอ่านผลด้วยการ **แกะไฟล์ region ของโลกที่ save แล้ว** แทน (ตัวเลขข้างบนมาจากตรงนั้น)
- [~] **9d** **Folia ท่อข้าม region** — **ทดสอบให้ไม่ได้จริง ๆ และไม่ใช่ข้อบกพร่อง:** ท่อต้องต่อกันไม่ขาด
  และยาวไม่เกิน `max-pipe-length` (64) ปลายทั้งสองจึงห่างกันไม่เกิน ~4 chunk ที่โหลดติดกันเสมอ
  ซึ่ง Folia จะรวมเป็น region เดียวกันตลอด ทดแทนด้วยท่อยาวข้าม 3 chunk ใน 9c แล้ว
  (โค้ดยังเรียก `runAtLocation` แยกต่อ output อยู่ดี เส้นทางเดียวกันทั้งสองกรณี)
- [x] **9e** ปลายทางอยู่ chunk ที่ไม่ได้ force-load: กด pulse → **ไม่มี error** และของครบ
  (ปลายทาง 32→64, ต้นทางลด 32 พอดี) — Bukkit โหลด chunk ให้เองตอนอ่านบล็อก จึงย้ายได้ตามปกติ

---

### หมายเหตุการนับของ (§6)
วิธีนับที่เชื่อถือได้: ใช้คำสั่ง/แก้ inventory ให้จำนวนเริ่มต้นชัด แล้ว `/gamerule`ปิด hopper ข้างเคียง, กด pulse เป็นจำนวนรอบที่รู้ค่า, รวมของทั้ง source + dest + ที่ drop พื้น → ต้องเท่ากับจำนวนเริ่มต้นเสมอ ไม่ว่ากรณีใด (นี่คือ invariant หลักของ plugin: ของที่ดูดออกมาถูก insert / คืน / หรือ drop เสมอ — สร้างเพิ่มไม่ได้)
